services:
  app:
    build: .
    volumes:
      - ..:/workspace
    command: sleep infinity
    depends_on:
      - db

  db:
    image: mongo:7
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db

volumes:
  mongo-data:
